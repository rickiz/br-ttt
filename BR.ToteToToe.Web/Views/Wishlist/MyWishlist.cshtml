@using BR.ToteToToe.Web.ViewModels
@model WishlistIndexViewModel
@{
    ViewBag.Title = "My Wish List Test";
}

<div id="mainbody">
    <div id="wrap"><br />
	    <div class="copy">
	        <div class="formbox">
            <form id="form1" name="form1" method="post" action="">
                <table width="100%" border="0" cellspacing="0" cellpadding="0">
                <tr>
                    <td align="left" style="font-size: 18px; color: #666;">Your Wish List<br />
                    <hr class="divline" /></td>
                </tr>
                <tr>
                    <td>
                    <div id="wishListResults">
                        @Html.Partial("_WishlistItemList", Model)
                    </div>
                    </td>
                </tr>
                </table>
            </form>
            </div>
	    </div>
    </div>
</div>
@*<div id="wrap"><br />
    <form id="form1" name="form1" method="post" action="">
        <div id="wrap2">
            <table width="86%" border="0" align="right" cellpadding="0" cellspacing="0">
                <tr>
                    <td align="left" style="font-size: 18px; color: #666;">Your Wish List<br />
                    <hr class="divline" /></td>
                </tr>
                <tr>
                    <td>
                        <div id="wishListResults">
                            @Html.Partial("_WishlistItemList", Model)
                        </div>
                    </td>
                </tr>
            </table>
            <br />
        </div>
    </form>
</div>*@

@section scripts
{
    <script>
        function RemoveWishlistItem(id) {
            var modelID = $('#ModelID').val();

            var url = '@(Url.Content("~/Wishlist/Remove"))';
            $.post(url, { wishlistItemID: id }, function (result) {
                if (result != '') {
                    $("#wishListResults").html(result);
                }
                else {
                    //fail
                }
            });
        }
    </script>

}

@section styles
{
    <link href="@(Links.Content.wishlist_css)" rel="stylesheet" type="text/css" />
    <link href="@(Links.Content.ttt_css)" rel="stylesheet" type="text/css" />
    <link href="@(Links.Content.form_css)" rel="stylesheet" type="text/css" />
}

@section fbheader
{
    <meta property="fb:app_id" content="267986850024373" />
    <meta property="og:title" content="@ViewBag.Title" />
    <meta property="og:url" content="http://www.tote-to-toe.com/Wishlist/MyWishlist/@Model.WishlistID" />
    <meta property="og:image" content="http://www.tote-to-toe.com/images/main_pic4.jpg" />
    <meta property="og:description" content="@ViewBag.Title share dialog. (testing)" />
}
